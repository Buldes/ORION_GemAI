# ORION_GemAI

## Settup for GPu.Usage
1. Run ```./configure_for_gpu.cmd```
2. Download and Install Cuda 12.9 ( **NO OTHER VERSION!** )
3. Set ``"cuda_dir"`` in ```./assets/settings.json``` if changed
4. Change ``"tts_device"`` to ``gpu``
5. Run programm